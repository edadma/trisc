/*
 * TOS System Services
 *
 * User-facing API for TOS kernel services.
 * Import this module to use sleep, putc, yield, exit.
 *
 * Syscall numbers:
 *   0 = sleep    — block for N timer ticks
 *   1 = putc     — write character to stdout
 *   2 = yield    — voluntary context switch
 *   3 = exit     — terminate current thread
 */

extern syscall(number: int, arg: int) -> int

val SYS_SLEEP = 0
val SYS_PUTC  = 1
val SYS_YIELD = 2
val SYS_EXIT  = 3

sleep(ticks: int)
    syscall(SYS_SLEEP, ticks)

putc(ch: int)
    syscall(SYS_PUTC, ch)

yield()
    syscall(SYS_YIELD, 0)

exit()
    syscall(SYS_EXIT, 0)
