hello — print a greeting
=========================

A simple standalone program that prints a message and exits.
Uses one [[puts]] to the TTY server so the line goes out as a single IPC
payload (not one [[putc]] syscall per character).

    module oskit.bin.hello

    import oskit.ulib.{puts, exit}

    main()
        puts("Hello, world!\n")
        exit()
