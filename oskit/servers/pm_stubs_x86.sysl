// Stub PM client functions for x86_64 (no PM server running).
// All calls return -1 (failure).

module oskit.servers

pm_waitpid(pid: int) -> int
    -1

pm_kill(pid: int) -> int
    -1

pm_spawn_handles(path: string, args: string, hmap: *byte, num: int) -> int
    -1
