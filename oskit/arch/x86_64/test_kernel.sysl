// Minimal test kernel for x86_64 boot.

extern uart_putc(c: char)

uart_puts(s: string) = for c in s do uart_putc(c)

kernel_init()
    uart_puts("kernel_init: ok\n")

kernel_main() -> i64
    uart_puts("kernel_main: SLIX alive on x86_64!\n")
    i64(0)
