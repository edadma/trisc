/* echo — write arguments to standard output */
import std.process.*

main() -> int
    val n = argc()
    var first = true

    for i in 1..<n
        if !first then putchar(' ')

        puts(argv(i))
        first = false

    putchar('\n')
    0
