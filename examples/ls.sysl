import "std/fs"
import "std/process"

main() -> int
    dir = "."
    if argc() > 0
        dir = argv(0)
    entries = readdir(dir)
    for i = 0; i < len(entries); i++
        puts(entries[i].name)
        putchar('\n')
    0
