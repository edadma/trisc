// Array demo — fill an array and compute the sum
main() -> int
    arr: [10]int
    for i = 0; i < 10; i++
        arr[i] = (i + 1) * (i + 1)
    sum = 0
    for i = 0; i < 10; i++
        sum += arr[i]
    sum  // 1 + 4 + 9 + 16 + 25 + 36 + 49 + 64 + 81 + 100 = 385
