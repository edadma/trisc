// Multi-file demo — uses math module
import math.*

main() -> int
    a = abs_val(-42)           // 42
    b = max_of(10, 20)         // 20
    c = min_of(a, b)           // 20
    a + b + c                  // 82
