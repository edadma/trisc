// Iterative Fibonacci — returns the Nth Fibonacci number
fib(n: int) -> int
    var a: int = 0
    var b: int = 1
    for i = 0; i < n; i++
        var t: int = b
        b = a + b
        a = t
    a

main() -> int = fib(10)
