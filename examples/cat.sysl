import "std/fs"
import "std/io"
import "std/process"

main() -> int
    if argc() < 1
        write_string(STDERR, "usage: cat <file>...")
        putchar('\n')
        return 1
    for i = 0; i < argc(); i++
        content = read_file(argv(i))
        write_string(STDOUT, content)
    0
